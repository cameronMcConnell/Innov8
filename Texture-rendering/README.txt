Create a texture from an image loaded on a surface

This program will take an image loaded from the SDL_image library (PNG)
and will create a texture from the surface that it is loaded on. Then it will
render the texture to the window and display the said texture.
