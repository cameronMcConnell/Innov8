This program will create a window using SDL, then using this window it will show 5 cat images
continuously with a three second interval between each. Once reaching the final cat image, the 
program will restart from the first image. The program will terminate when the user closes the 
window.
